# Logging configuration
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG level includes sensitive content (full diffs, LLM prompts/responses)
LOG_LEVEL=DEBUG

# Google Cloud / Vertex AI
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=europe-west2

# Generic variables (work for both GitHub and GitLab)
REPOSITORY=owner/repo
PR_NUMBER=1

# GitHub-specific (if using GitHub)
# GH_TOKEN is optional for local development:
# - CI/CD: Set GH_TOKEN (or use GITHUB_TOKEN automatically provided by GitHub Actions)
# - Local: Leave unset and authenticate via 'gh auth login' instead
GH_TOKEN=ghp_xxxxxxxxxxxx

# GitLab-specific (if using GitLab)
GITLAB_TOKEN=glpat-xxxxxxxxxxxx
CI_SERVER_HOST=gitlab.com

# Optional: Google Cloud credentials for CI/CD
# GOOGLE_CLOUD_CREDENTIALS=<service-account-json>